plugins {
    id 'java-library'
    id 'maven-publish'
    id 'signing'
}

group = 'net.zdechov.sharpmz'
ext.libraryName = 'Sharp MZ Emulator Graphics Device Library'
ext.libraryDescription = 'Graphics device library'

apply from: '../jmzemu-modules.gradle'

if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

repositories {
    mavenLocal()
    mavenCentral()
    flatDir {
        dirs "../../lib"
    }
}

dependencies {
    implementation moduleDep('XbupJava', 'org.exbin.xbup', 'modules', 'xbup-plugin')
    implementation moduleDep('Jmzemu', 'net.zdechov.sharpmz', 'modules', 'jmzemu-device-api')

    testImplementation group : 'junit', name: 'junit', version: '4.10'
}
