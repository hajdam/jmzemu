group = 'net.zdechov.sharpmz'
ext.libraryName = 'Sharp MZ Emulator GDG Device Library'
ext.libraryDescription = 'GDG device library'

apply from: '../jmzemu-modules.gradle'

buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
    dependencies {
        classpath("at.bxm.gradleplugins:gradle-svntools-plugin:latest.integration")
    }
}

if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

repositories {
    mavenLocal()
    mavenCentral()
    flatDir {
        dirs "../../lib"
    }
}

dependencies {
    implementation moduleDep('Jmzemu', 'net.zdechov.sharpmz', 'jmzemu-device-api')
    implementation moduleDep('Jmzemu', 'net.zdechov.sharpmz', 'jmzemu-device-memory-api')
    implementation moduleDep('Jmzemu', 'net.zdechov.sharpmz', 'jmzemu-device-gdg-api')
    implementation moduleDep('XbupJava', 'org.exbin.xbup', 'xbup-plugin')

    testImplementation group : 'junit', name: 'junit', version: '4.10'
}
